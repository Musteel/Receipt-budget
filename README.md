Receipt-budget

A simple receipt-to-budget web app: uses OCR to scan receipts, parse expense data, and suggest how to save money 🧾💰

🎯 What it does

Scans receipt images (e.g. photos of printed receipts) using OCR (Tesseract OCR / EasyOCR) to recognize text.

Extracts relevant expense information (e.g. vendor, date, amounts) from the receipt.

Stores and aggregates expenses to build a personal budget.

Provides simple analytics / visualization (e.g., total spending, category summaries) to highlight potential savings.

Optionally — classification of receipts to categories (using a trained classifier), aiding better expense tracking.
